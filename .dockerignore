# Prevent large/irrelevant files from bloating build context
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env and secrets (compose uses .env at runtime)
.env
.env.*

# Build outputs
/dist
logs
coverage

# VCS and IDE
.git
.gitignore
.vscode
.DS_Store

# Tests and fixtures (not needed in production image)
tests
scripts